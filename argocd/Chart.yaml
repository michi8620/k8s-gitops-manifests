apiVersion: v2
name: argocd
description: >
  ArgoCD self-managed umbrella chart.
  Installs ArgoCD itself plus AppProjects and ApplicationSets
  that drive the entire GitOps workflow.
type: application
version: 1.0.0
appVersion: "v3.3.6"

dependencies:
  - name: argo-cd
    version: "9.4.17"
    repository: https://argoproj.github.io/argo-helm
