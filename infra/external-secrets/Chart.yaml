apiVersion: v2
name: external-secrets
description: External Secrets Operator for syncing secrets from cloud providers
type: application
version: 1.0.0
appVersion: "0.12.0"
dependencies:
  - name: external-secrets
    version: "0.12.1"
    repository: https://charts.external-secrets.io
